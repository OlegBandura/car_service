<div class="admin_form">
  <%= form_for :break_category, url: admin_break_categories_path do |form| %>

    <p>Add new destroy category</p>
    <p>Name category
      <%= form.text_field :break_category_name %></p>
    <p><%= form.submit 'Add new destroy category'%></p>
  <% end %>
</div>
